{
  "version": 2,
  "builds": [
    {
      "src": "api/*.go",
      "use": "@vercel/go"
    }
  ],
  "routes": [
    {
      "src": "/deployments",
      "methods": ["GET", "POST"],
      "dest": "/api/deployments.go"
    }
  ]
}
